name: Listar arquivos

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  list-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Listar todos os arquivos
        run: |
          echo "Arquivos encontrados no:"
          ls -lah
